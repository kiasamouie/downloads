# Python 3.11+ recommended (3.10 still works but yt-dlp prints a deprecation notice).
# download.py itself only uses the standard library; its single dependency is the
# yt-dlp executable, which this line installs into the virtual environment.
yt-dlp==2026.08.19
