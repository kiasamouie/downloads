# Python virtual environment (contains the installed yt-dlp + ffmpeg binaries)
env/
.venv/

# Bundled standalone CPython used as the venv base on this machine (~350MB)
.py312/

# Local per-machine configuration (keep .env.example tracked)
.env

# Python bytecode / caches
__pycache__/
*.py[cod]
*.egg-info/

# OS / editor junk
.DS_Store
Thumbs.db
